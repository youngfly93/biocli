# gene-dossier-tp53 run summary

Used `biocli aggregate gene-dossier TP53 -f json` and `biocli gene search TP53 -f json` to confirm the canonical TP53 entity and collect pathway, interaction, and ClinVar context.

Result written to `run-001.json`.
